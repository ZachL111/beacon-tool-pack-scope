[project]
name = "beacon-tool-pack-scope"
version = "0.1.0"
description = "Package a Python local lab for pack analysis with log and snapshot fixtures, replay consistency checks, and documented operating limits."
requires-python = ">=3.11"
